import { describe, expect, it } from "vitest";

// Placeholder so `npm test` has something to run from the first commit.
// Replace with the rule-1 test in slice 4 — see PLAN.md.
describe("scaffold", () => {
  it("test runner is wired up", () => {
    expect(true).toBe(true);
  });
});
