# Supabase project (Postgres, EU/London region per CLAUDE.md).
# Project settings -> API. The anon key is safe to expose - RLS is what
# actually restricts what it can do (see supabase/migrations).
SUPABASE_URL=
SUPABASE_ANON_KEY=
